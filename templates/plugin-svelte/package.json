{
  "name": "uframe-plugin-callout-svelte",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "Example uframe plugin authored in Svelte 5, compiled to a self-contained custom element (framework-neutral contract).",
  "scripts": {
    "dev": "vite",
    "build": "vite build --config vite.config.build.ts",
    "typecheck": "svelte-check --tsconfig ./tsconfig.json"
  },
  "peerDependencies": {
    "uframe": "*"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
